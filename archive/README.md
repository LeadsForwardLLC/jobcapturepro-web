# Archive (legacy)

This folder contains **legacy CSS/JS** that is **not loaded** by the theme. The active theme uses `css/` and `assets/js/` (and related assets) only. This archive is kept for reference only; safe to remove before publishing to a theme library if you want a minimal package.
